{
  "scripts": {
    "build": "./node_modules/.bin/babel src --out-dir build --delete-dir-on-start --copy-files && node build.js",
    "watch": "node build.js && ./node_modules/.bin/babel src --out-dir build --copy-files --watch"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0"
  }
}

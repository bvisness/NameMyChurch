{
  "scripts": {
    "build": "./node_modules/.bin/babel src -d build && node build.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0"
  }
}
